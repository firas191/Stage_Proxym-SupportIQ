package com.supportiq.backend.imports;

/** Formats d'entree (rapport §4). Structures : CSV/XLSX/JSON/TXT. Non structures (S7) : PDF/DOCX/EML. */
public enum FileType {
    CSV,
    XLSX,
    JSON,
    TXT,
    PDF,
    DOCX,
    EML
}
